# AC: title-format

**Requirement:** feature#req:title-format

The first line of a feature README is a level-1 heading with the `Feature:` prefix. A README whose title does not match `# Feature: {Title}` is rejected by validation.
