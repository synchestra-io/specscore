# AC: contents-when-children

**Requirement:** feature#req:contents-when-children

A feature with child directories includes a Contents section containing an index table and a brief summary for each child. A feature with children but no Contents section is rejected by validation.

**See also:** [underscore-reserved](underscore-reserved.md) (`_`-prefixed directories are not children and are excluded from Contents)
